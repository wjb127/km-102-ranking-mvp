# 작업 로그 — 2026-04-29

계약 기반 QA + 클라이언트 피드백 반영. 순서대로 정리.

---

## 1. P1 — GOAT 푸터 합계 미갱신

**증상**: 투표 후 카테고리 푸터 총합이 즉시 반영 안 됨.

**원인**: SWR `mutate` 1개만 호출. `categoriesRes` 캐시 무효화 누락.

**수정**: `src/app/vote/page.tsx`
- `mutate: mutateCategories` 캡처
- POST 성공/실패 양쪽에서 `mutateCategories()` 호출
- `useCallback` deps에 추가

---

## 2. P2 — 모바일 하단 패딩 부족

**증상**: 하단 탭바가 콘텐츠 가림. iOS safe-area 미반영.

**수정**: `src/app/layout.tsx`
- `pb-20 md:pb-0 md:pt-16`
- → `pb-[calc(4rem+env(safe-area-inset-bottom)+1rem)] md:pb-0 md:pt-16`

---

## 3. 프로덕션 재검증

- 임시 도메인: https://mma-ranking-community.vercel.app
- Playwright MCP로 7장 추가 스크린샷
- `qa-report/report.html` Fix 재검증 섹션 + 통계 갱신 (28→35장, 4/6 P1·P2 해결)
- 역질문 섹션 제거 (피드백 반영)

---

## 4. 한글 번역 수기 매핑 (클라이언트 피드백)

**문제**: DeepL 자동 번역이 팬 통용 표기와 불일치.
- 예: "Prates" → "프레이츠" (X) → "프라치스" (O)

**대응**: 검색 + 수기 매핑 스크립트 5종 작성.

### 4-1. `scripts/manual-translate-fighters.ts`

선수 영문 → 한글 (팬 통용 표기). NAME_MAP 195+ 엔트리.

| 영역 | 예시 |
|------|------|
| GOAT/탑 랭커 | Conor McGregor → 코너 맥그리거, Khabib → 하빕 누르마고메도프 |
| 챔피언 | Jon Jones → 존 존스, Israel Adesanya → 이스라엘 아데산야 |
| 여성 | Ronda Rousey → 론다 로우지, Zhang Weili → 장 웨이리 |
| 한국 파이터 | Chan Sung Jung → 정찬성, Doo Ho Choi → 최두호, Dong Hyun Kim → 김동현 |
| Bellator/ONE | Patricio Pitbull → 파트리시오 피트불, Michael Chandler → 마이클 챈들러 |

**결과**: 177건 갱신.

### 4-2. `scripts/fix-events-and-extra.ts`

UFC PPV 이벤트 + 베뉴 + 추가 선수.

- EXTRA_FIGHTERS 6건 (Carlos Prates → 카를루스 프라치스, Jack Della Maddalena 등)
- EVENT_MAP 15건 (UFC 311~323 PPV)
- venue 11종 (T-모바일 아레나, 매디슨 스퀘어 가든, 에티하드 아레나 등)

**결과**: fighters 6건, events 13건, venues 35건.

### 4-3. `scripts/auto-event-translate.ts`

라스트네임 매핑 + 정규식으로 이벤트 타이틀 자동 변환.

- LAST_NAME_KO 240+ 엔트리
- 패턴 `^UFC(?:\s+\d+|\s+Fight\s+Night|\s+Freedom\s+\d+)?:\s*A\s+vs\.?\s+B\s*N?$`
- 특수 케이스: "UFC 306 – Riyadh Season Noche UFC: ..." em-dash 부제
- curly apostrophe(`'`) 정규화

**결과**: 4회 실행 누적 약 124건 변환, 매핑 실패 0건.

### 4-4. 보조 스크립트

- `scripts/search-name.ts` — fighters 영문/한글 ILIKE 검색
- `scripts/search-event.ts` — events + fights 조인 검색

---

## 5. 빌드 / 배포

- `pnpm build` 통과 (TypeScript strict 통과, Drizzle `RowList` 타입 캐스팅 보강)
- master 푸시: `dc38611..8663cad`
- Vercel 자동 배포 트리거됨

---

## 커밋 히스토리 (이번 세션)

```
8663cad fix(i18n): 선수/이벤트/베뉴 한글 표기 수기 매핑
dc38611 docs(qa): 역질문 섹션 제거
d6de9b9 docs(qa): prod Fix 재검증 섹션 + 스크린샷 7장 추가
93a6e19 fix: 계약 기반 QA 발견 이슈 + QA 리포트 추가
```

---

## 데이터 영향

- `fighters.full_name_ko`: 183건 갱신
- `events.name_ko`: 124+건 갱신
- `events.venue_ko`: 35건 갱신
- 잔여 미매핑 UFC vs 이벤트: 0건

---

## 다음 액션 후보

- Vercel 배포 완료 후 프로덕션 재크롤
- 신규 이벤트 자동 동기화 시 LAST_NAME_KO 보강 워크플로우 정립
- 닉네임 한글화(`fighters.nickname_ko`) 별도 패스 필요시 동일 패턴 적용
